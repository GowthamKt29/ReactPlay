/**
 * @format
 */

import {AppRegistry} from 'react-native';
import StateExp from './Basics/StateExp';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => StateExp);
